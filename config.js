var config = {};
config.maxBots = 50;//Default=50. If using VPS, max=300 or lag. If not using VPS, max=100 or lag.
config.serverPort = 8081;//Default=8081
config.proxies = "proxy.txt";//Default=proxy.txt
config.chatMsg = "Free Snow at www.olaf4snow. com";//Default=Free Snow at www.olaf4snow. com

config.botSkin = "[Olaf]";//Default=[Olaf]
config.botName = "4snow.com";//Default=4snow.com

module.exports = config;